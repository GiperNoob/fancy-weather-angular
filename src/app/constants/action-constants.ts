export enum FancyWeatherActions {
  SET_BACKGROUND_IMAGE = '[SET BACKGROUND IMAGE ACTION] set background image',
  GET_SRC_IMG_ACTION = '[GET SRC IMG ACTION] get request for src img',
  GET_IP_DATA_ACTION = '[GET IP DATA ACTION] get request IP for init app',
  SET_INIT_DATA_ACTION = '[SET IP DATA ACTION] set init data for app(AGM, GeoInfo)',
  GET_WEATHER_ACTION = '[GET WEATHER ACTION] get weather',
  SET_WEATHER_ACTION = '[SET WEATHER ACTION] set weather',
}
